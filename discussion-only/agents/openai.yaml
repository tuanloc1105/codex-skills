interface:
  display_name: "Discussion Only"
  short_description: "Discuss with an auto-saved Markdown tracker"
  default_prompt: "Use $discussion-only to discuss this topic without source-code changes, establish the existing-behavior baseline and regression safeguards before recommending changes, and auto-save the tracker without asking storage questions."

policy:
  allow_implicit_invocation: false
