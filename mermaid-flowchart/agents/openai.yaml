interface:
  display_name: "Mermaid Flowchart"
  short_description: "Turn session context into validated Mermaid flows"
  default_prompt: "Use $mermaid-flowchart to diagram the process we just discussed."
policy:
  allow_implicit_invocation: true
