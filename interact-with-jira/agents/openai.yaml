interface:
  display_name: "Interact with Jira"
  short_description: "Use Jira MCP first, with approved ACLI fallback"
  default_prompt: "Use $interact-with-jira to handle this Jira Cloud task through Atlassian MCP first, and ask before falling back to ACLI."
