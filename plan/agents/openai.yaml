interface:
  display_name: "Plan"
  short_description: "Discuss, approve, and auto-save execution plans"
  default_prompt: "Use $plan to inspect the task and save one version 4 plan bundle, keeping approval separate from authorization to execute."
