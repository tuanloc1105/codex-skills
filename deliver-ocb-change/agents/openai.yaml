interface:
  display_name: "Deliver OCB Change"
  short_description: "Deliver OCB work through Tech Lead-approved merge"
  default_prompt: "Use $deliver-ocb-change to take this OCB Jira task through planning, implementation, domain verification, Tech Lead approval, and a verified developer-performed merge."

policy:
  allow_implicit_invocation: true
