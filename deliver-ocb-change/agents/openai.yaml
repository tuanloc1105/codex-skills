interface:
  display_name: "Deliver OCB Change"
  short_description: "Deliver OCB work through a verified merge"
  default_prompt: "Use $deliver-ocb-change to take this OCB Jira task through planning, LinearB-visible implementation, domain verification, and a verified developer-performed merge."

policy:
  allow_implicit_invocation: true
