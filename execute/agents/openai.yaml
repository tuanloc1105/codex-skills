interface:
  display_name: "Execute"
  short_description: "Execute plans with smallest verified commits"
  default_prompt: "Use $execute to adopt the version 4 execution bundle at /path/to/record/, read index.md and its manifest, execute dependency-ready phase files, update evidence and verification through record write transactions, and remain active until I explicitly exit execute."
