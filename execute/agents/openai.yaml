interface:
  display_name: "Execute"
  short_description: "Execute approved work with evidence and recovery"
  default_prompt: "Use $execute to resume the approved bundle at /path/to/record/, verify the authorized work, and preserve accurate evidence through completion, pause, or recovery."
