interface:
  display_name: "Discuss"
  short_description: "Discuss with resumable decisions and evidence"
  default_prompt: "Use $discuss to keep a concise resumable record, resolve material decisions, and respect an explicit pause, exit, or plan/execute handoff."

policy:
  allow_implicit_invocation: false
