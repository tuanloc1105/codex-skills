{
  "version": "v1",
  "hooks": [
    {
      "name": "Workflow modes session anchor",
      "description": "Restore active tracker-backed workflow state at session start.",
      "trigger": "SessionStart",
      "action": {
        "type": "command",
        "command": "{{WORKFLOW_MODES_COMMAND}}"
      },
      "timeout": 3
    },
    {
      "name": "Workflow modes prompt guard",
      "description": "Inject the active workflow anchor and block prompts that cannot reconcile state.",
      "trigger": "UserPromptSubmit",
      "action": {
        "type": "command",
        "command": "{{WORKFLOW_MODES_COMMAND}}"
      },
      "timeout": 3
    },
    {
      "name": "Workflow modes tool guard",
      "description": "Enforce record synchronization, transactional writes, and scoped mutation actions.",
      "trigger": "PreToolUse",
      "matcher": ".*",
      "action": {
        "type": "command",
        "command": "{{WORKFLOW_MODES_COMMAND}}"
      },
      "timeout": 3
    },
    {
      "name": "Workflow modes stop recovery",
      "description": "Persist warning-only recovery state when Kiro cannot block Stop.",
      "trigger": "Stop",
      "action": {
        "type": "command",
        "command": "{{WORKFLOW_MODES_COMMAND}}"
      },
      "timeout": 3
    }
  ]
}
